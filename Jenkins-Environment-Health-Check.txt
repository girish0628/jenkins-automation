pipeline {
    agent { label 'windows' }
    stages {
        stage('Run Validation') {
            steps {
                bat 'python validator.py config.json'
            }
        }
    }
    post {
        always {
            archiveArtifacts artifacts: 'reports/*.json'
        }
    }
}